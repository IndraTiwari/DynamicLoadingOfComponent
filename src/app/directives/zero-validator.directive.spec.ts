import { ZeroValidatorDirective } from './zero-validator.directive';

describe('ZeroValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new ZeroValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
